# ⚡ Ускорение запуска + автопереключение на режим 5 после сброса

## Что было исправлено

### 1. Ускорение запуска (с 10-15 сек на 1-2 сек)
- ✅ **Асинхронное подключение** — USB инициализация в отдельном потоке
- ✅ **Таймаут 3 сек** на создание USBStream (вместо зависания)
- ✅ **Неблокирующий аппаратный сброс** — GPIO операции в фоне
- ✅ **Быстрые проверки на старте** — таймаут зависания 1 сек (вместо 2 сек)

### 2. Автопереключение на режим 5 после авторесета
- Когда срабатывает авторесет (зависание потока), программа:
  1. Выполняет сброс STM32
  2. Переподключается к устройству
  3. **Автоматически переключается на режим 5** (вместо того чтобы оставаться на прежнем режиме)
  4. На режиме 5 параметры не восстанавливаются (займемся позже)

## Временная шкала запуска

```
t=0 сек:    Пользователь нажимает "Run"
t=0-0.2 сек: GUI готов, мигает "Подключение..."
t=0-3 сек:   Попытка подключения (в фоне, не блокирует UI)
t=1-2 сек:   Если зависание → авторесет
t=2-3 сек:   Переподключение → переключение на режим 5
t=3+ сек:    Начинают поступать данные, график оживает
```

## Поведение при сбое

**Сценарий:** Устройство не отвечает при подключении

1. t=0-0.2 сек: Вы видите интерфейс (уже отзывчивый)
2. t=1 сек: "Поток пропал — сброс STM32…"
3. t=1.1 сек: SOFT_RESET через CDC
4. t=1.2 сек: DEEP_RESET через bulk  
5. t=1.5 сек: Аппаратный сброс GPIO17 (0.5 сек + импульс)
6. t=2 сек: Переподключение
7. t=2-3 сек: Переключение на режим 5
8. t=3+ сек: Данные должны начать поступать

## Отключение/Настройка

```bash
# Отключить авторесет на старте
export BMI30_HW_RESET_ON_START=0

# Если нужен старый таймаут (3 сек вместо 0.5 сек после GPIO)
export BMI30_HW_RESET_WAIT_S=3.0

# Если нужен старый таймаут зависания (2 сек вместо 1 сек)
export BMI30_STALL_RESET_AFTER=2
```

## Тестирование

### Тест 1: Быстрый запуск
```bash
./.usbvenv/bin/python host/BMI30.200.py.2026-02-07-autoreset
# Проверьте: UI становится отзывчивым в течение 0.2 сек
```

### Тест 2: Авторесет при зависании
```bash
# Запустить программу
./.usbvenv/bin/python host/BMI30.200.py.2026-02-07-autoreset

# Отключить USB устройство (или имитировать зависание)
# Должно произойти:
# 1. "Поток пропал — сброс STM32…"
# 2. Переключение на кнопку 5
# 3. [CONNECT] Auto-reset occurred, switching to mode 5
```

### Тест 3: Норма вал ное подключение
```bash
./.usbvenv/bin/python host/BMI30.200.py.2026-02-07-autoreset
# Если устройство отвечает нормально в течение 1 сек,
# то автопереключения на режим 5 не будет (флаг не установится)
```

## Файлы изменены
- `host/BMI30.200.py.2026-02-07-autoreset`

## Что дальше
- ✏️ Восстановление параметров после авторесета (потом)
- ✏️ Более точное определение конца сброса
- ✏️ Опциональное сохранение режима перед авторесетом
